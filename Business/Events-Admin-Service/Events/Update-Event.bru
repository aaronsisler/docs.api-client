meta {
  name: Update-Event
  type: http
  seq: 3
}

put {
  url: {{events_admin_service_url}}/clients/{{events_admin_service_client_id}}/events
  body: json
  auth: none
}

body:json {
  {
    "clientId": "{{events_admin_service_client_id}}",
    "eventId": "{{events_admin_service_event_id}}",
    "createdOn": "{{events_admin_service_event_created_on}}",
    "name": "Bob's Pizza"
  }
}
